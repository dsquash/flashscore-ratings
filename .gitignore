# ── After Effects project & footage ──────────────────────────────
*.aep
*.aep_AME
(Footage)/

# ── Match output (per-match data, images, debug files) ───────────
flashscore_output/

# ── Python cache ─────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Files marked for deletion ────────────────────────────────────
POATE FI STERS*/

# ── OS / editor noise ────────────────────────────────────────────
.DS_Store
Thumbs.db
*.tmp
*.bak

# ── Local, nu se publica ─────────────────────────────────────────
# CLAUDE.md e documentatie locala; a continut secrete in trecut
CLAUDE.md
.claude/
# Copia de arhiva a template-ului curat (AE + Collect Files)
_template-master/
